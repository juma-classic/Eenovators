{
  "name": "eenovators",
  "version": "0.1.0",
  "private": true,
  "description": "Static site for Eenovators",
  "scripts": {
    "dev": "live-server --port=3000 --open=./index.html --host=0.0.0.0",
    "start": "live-server --port=3000 --open=./index.html --host=0.0.0.0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
